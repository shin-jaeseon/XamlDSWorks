<UserControl x:Class="XamlDS.Showcases.ItkThemes.Views.Controls.Sliders.SlidersPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:itk="http://xamldesignstudio.com"
             xmlns:local="clr-namespace:XamlDS.Showcases.ItkThemes.Views.Controls.Sliders"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="Slider" x:Key="CustomSliderStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Border>
                            <Grid>
                                <Border Height="6" VerticalAlignment="Center" CornerRadius="3" Background="Yellow" />

                                <Track x:Name="PART_Track" VerticalAlignment="Center">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="Slider.DecreaseLarge" Style="{x:Null}" Background="Transparent" IsTabStop="False" />
                                    </Track.DecreaseRepeatButton>

                                    <Track.Thumb>
                                        <!-- Custom Thumb -->
                                        <Thumb Width="20" Height="20">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Ellipse Fill="#4CAF50" Stroke="White" StrokeThickness="2"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>

                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="Slider.IncreaseLarge" Style="{x:Null}" Background="Transparent" IsTabStop="False" />
                                    </Track.IncreaseRepeatButton>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource BodyPanelStyle}" Background="#F222">
        <Grid>
            <itk:GridLinePane GridOpacity="0.05"/>
            <Slider Width="256" VerticalAlignment="Center" HorizontalAlignment="Center" Minimum="0" Maximum="100" Style="{StaticResource CustomSliderStyle}">
            </Slider>
        </Grid>
    </Border>
    
</UserControl>
