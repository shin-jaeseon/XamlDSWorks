<UserControl x:Class="XamlDS.DemoApp.Views.AppBottomBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XamlDS.DemoApp.Views"
             mc:Ignorable="d" d:DesignHeight="48" d:DesignWidth="800">
    <!--<UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Width" Value="120"/>
        </Style>
    </UserControl.Resources>-->
    <DockPanel Margin="45,0">
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
            <Button DataContext="{Binding ToggleFullscreenWindowCvm}" Content="{Binding DisplayName}" Command="{Binding Command}" Width="128"/>
            <Button Content="Quit" Command="{Binding QuitApplicationCvm.Command}" Width="128"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Left"  Orientation="Horizontal">
            <Button Content="{Binding OpenHomePanelCvm.DisplayName}" Command="{Binding OpenHomePanelCvm.Command}" Width="128"/>
        </StackPanel>
        <Border/>
    </DockPanel>
</UserControl>
